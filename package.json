{
  "name": "Figma-Pivotal",
  "version": "1.0.0",
  "description": "Your Figma widget",
  "scripts": {
    "build": "esbuild widget-src/code.ts --bundle --outfile=dist/code.js --target=es6",
    "tsc": "tsc --noEmit -p widget-src",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "@figma/widget-typings": "*",
    "esbuild": "*",
    "typescript": "*"
  },
  "dependencies": {
    "@types/ramda": "^0.29.10",
    "date-fns": "^3.3.1"
  }
}
