{
  "name": "figma-pivotal",
  "version": "1.0.0",
  "description": "Figma plugin to integrate with Pivotal Tracker",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es6",
    "tsc": "tsc --noEmit -p src",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.2",
    "@figma/widget-typings": "^1.9.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
